---
export interface Props {
	name: string;
	id: string;
	value: string;
	label: string;
}

const {name, id, value, label} = Astro.props;
---

<div class='option'>
	<input type='checkbox' name={name} id={id} value={value} />
	<label for={id}>{label}</label>
</div>
